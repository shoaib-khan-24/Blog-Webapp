# Blog-Webapp
A fully-functional blogging website built upon python, flask, authentication, SQLAlchemy and many more. 

Run the following command to install all required dependencies:
`python -m pip install -r requirements.txt`
